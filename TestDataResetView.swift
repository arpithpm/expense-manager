// Test file to verify DataResetView can be compiled
import SwiftUI

struct TestDataResetView: View {
    var body: some View {
        DataResetView()
    }
}

struct TestDataResetView_Previews: PreviewProvider {
    static var previews: some View {
        TestDataResetView()
    }
}
